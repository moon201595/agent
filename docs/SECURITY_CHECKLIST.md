# 보안 체크리스트

2026-09-16 기준(Codex 작성, Claude 연결·수정). 테스트가 호출하는 운영 함수와 현재 구현 상태를 적는다. 실제 외부
네트워크·SMTP·Docker 운영 실측은 이 문서만으로 확인한 것이 아니므로 미실측으로 남긴다.

| 항목 | 무엇을 | 어디서(파일:함수) | 어떤 테스트가 지키는가 | 미구현이면 미구현 |
|---|---|---|---|---|
| PDF 크기·시간 상한 | 두 PDF 경로가 `MAX_PDF_BYTES=30 MiB`를 Content-Length와 `aiter_bytes()` 누적 모두에 적용하고, 스트림 전체에 `MAX_PDF_DOWNLOAD_SECONDS=120`초를 적용한다. | `server.py:_read_pdf_stream`, `_download_pdf_bytes`, `_download_oa_pdf_bytes`, `fetch_paper`, `fetch_pdf_from_url` | `test_security_limits.py:test_arxiv_pdf_content_length_is_rejected_before_body_read`, `test_arxiv_pdf_without_content_length_is_rejected_while_streaming`, `test_oa_pdf_stream_limit_is_enforced_without_content_length`, `test_oa_download_has_total_time_limit` | 실제 30 MiB 경계와 실제 느린 외부 서버 실측은 미실측 |
| 리다이렉트·SSRF | OA PDF 리다이렉트를 수동 추적하고 각 다음 URL을 검사하며, 최종 URL은 HTTPS이고 IP 리터럴·localhost·`.local` 대상이 아니어야 한다. | `server.py:_validate_pdf_redirect_url`, `_download_oa_pdf_bytes`, `fetch_pdf_from_url` | `test_oa_redirect_to_private_ip_is_rejected_before_following`, `test_oa_final_http_url_is_rejected` | DNS 해석 후 사설 주소 판정은 미구현(요청 범위 밖) |
| 저장소 크기 상한 | GitHub `gh api`의 `size`가 `MAX_REPO_KB=512000`을 넘으면 clone 전에 `repo_too_large`로 거부하고, gh 실패·비GitHub는 clone 뒤 `du -sk`로 확인한다. partial clone, submodule, LFS smudge도 제한한다. | `docker_runner.py:_github_repo_size_kb`, `_git_supports_blob_filter`, `_du_size_kb`, `_clone` | `test_clone_rejects_github_repo_before_git_when_api_size_is_too_large`, `test_clone_uses_filter_lfs_skip_and_post_clone_du`, `test_clone_removes_post_clone_repo_when_du_exceeds_limit` | GitHub API·실제 LFS 저장소·실제 디스크 압박 실측은 미실측 |
| 컨테이너 격리 플래그 | capability 전부 제거, no-new-privileges, pids·read-only·tmpfs·nobody를 기존 실행 경로에 적용한다. | `docker_runner.py:_SECURITY_FLAGS`, `_run_container` | 기존 `test_docker_hardening.py:test_drops_all_capabilities`, `test_blocks_privilege_escalation`, `test_limits_pids_against_fork_bomb`, `test_root_filesystem_is_read_only`, `test_runs_as_non_root`, `test_network_disabled_by_default` | 실제 Docker daemon에서의 공격 경로 실측은 미실측 |
| 헤드리스 LLM 도구 차단 | 헤드리스 에이전트 실행의 허용 도구와 read-only 인자를 기존 코드에서 강제한다. | `agent_maintenance.py`의 헤드리스 실행 경로 | 기존 `test_agent_maintenance.py:test_cli_children_get_no_secrets_and_no_tools` | 이번 작업에서는 기존 구현을 변경하지 않음 |
| 메일 링크 허용 목록 | HTTPS·userinfo 없음·비IP·비localhost·허용 호스트 또는 그 서브도메인·2000자 이하만 원문 그대로 허용한다. | `link_policy.py:safe_link` | `test_safe_link_rejects_unsafe_mail_urls`, `test_safe_link_preserves_allowed_url_and_subdomain` | `digest.paper_link` 가 arXiv·DOI 가 아닌 `open_access_pdf` 링크에 `safe_link` 를 적용한다(2026-09-16, `test_digest.py:test_paper_link_uses_doi_when_not_arxiv`). 미통과 링크는 메일에서 뺀다 |
| 인젝션 의심 표시 | 논문 본문을 차단하지 않고 의심 패턴을 표시해 후속 판단에 신호로 남긴다. | `injection_scan.py:scan` 및 `server.py:fetch_paper` 저장 경로 | 기존 `test_injection_scan.py` 관련 회귀 테스트 | 표시 후 자동 차단은 미구현 |
| 시크릿 비노출(.env 미읽기) | 작업 중 `.env` 내용을 읽거나 출력하지 않고, 로그·메일·외부 LLM 입력에 시크릿을 넣지 않는다. | 저장소 작업 규칙 `CLAUDE.md:규칙 5`, 기존 환경변수 참조 경로 | 전용 회귀 테스트는 미실측 | 규칙 5 의 "읽지 않는다"는 **에이전트(Claude·Codex)** 가 대화·로그에 시크릿을 노출하지 않는다는 뜻이다 — 프로그램이 `.env` 를 로드하는 것(`summarize_engine.load_env`)은 정상이다. 헤드리스 LLM 자식에게는 `agent_maintenance._cli_env` 가 HOME·PATH 만 넘긴다(`test_cli_children_get_no_secrets_and_no_tools`) |
| 반응 버튼 서명 | 회차·수신자별 반응 링크를 HMAC 서명하고 전달된 링크만 기록한다. | `feedback_links.py:new_issue_id`, `issue_links`, `mark_delivered` | 기존 `test_feedback_links.py` | 실제 공개 웹앱 배포·키 교체 실측은 미실측 |
| 새벽 메일 부재 알림 | 운영일 종료 줄, daily 프로필의 `last_digest_at`, 로그 `delivery`를 대조하고, 문제면 첫 daily 프로필 수신자에게 한 번 알린다. `--dry-run`은 발송하지 않는다. | `scripts/check_daily_mail.py:_completed_runs`, `_database_state`, `_check_run`, `_send_alert`, `main` | `test_daily_mail_check_returns_zero_when_scan_db_and_delivery_are_current`, `test_daily_mail_alert_uses_first_daily_recipients_and_suppresses_duplicate`, `test_daily_mail_dry_run_reports_problem_without_sending_or_stamping` | crontab `30 6 * * *` 연결(2026-09-16). 알림 수신자는 모든 daily 프로필에 공통인 주소(운영자). 실제 SMTP 수신은 미실측 |
